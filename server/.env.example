# Supabase environment variables (exemplo)
# Substitua os valores abaixo pelos valores reais do seu projeto Supabase.

SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key    # Use somente no servidor; NÃO comite em repositório público

# Onde obter os valores:
# 1) Abra o painel do seu projeto em https://app.supabase.com
# 2) Vá em Settings -> API
# 3) Copie o "Project URL" para SUPABASE_URL
# 4) Copie a "anon" key para SUPABASE_ANON_KEY (uso em cliente)
# 5) Copie a "service_role" key para SUPABASE_SERVICE_ROLE_KEY (apenas server)

# Não comite o arquivo .env com chaves reais. Copie este arquivo para `.env` e preencha as chaves reais.
